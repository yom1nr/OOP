// .equals(s) เช็คว่าเหมือนกันไหม (ห้ามใช้ ==)
// .length() หาความยาวข้อความ
// .trim() ตัดช่องว่างหน้า-หลัง
// .split(",") หั่นข้อความตามตัวคั่น (ได้ Array)
// Integer.parseInt(s) แปลงข้อความ "123" เป็นเลข 123
// try { ... } พื้นที่เสี่ยงภัย (ใส่โค้ดที่อาจพัง)
// catch (Type e) { ... } พื้นที่รับมือปัญหา (ทำเมื่อเกิด Error)
// finally { ... } ทำเสมอ ไม่ว่าจะ Error หรือไม่
// throw new ... สั่ง ให้เกิด Error (โยนระเบิด)
// throws ... เตือน ว่าเมธอดนี้อาจมี Error (แปะป้ายระวังระเบิด)